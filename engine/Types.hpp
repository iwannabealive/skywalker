#pragma once
#include <string>
#include <unordered_map>
#include <vector>
#include <memory>

using TextureHandle = int;   // placeholder handle
using AudioHandle   = int;

struct Vec2 {
    float x{0.0f};
    float y{0.0f};
};
